
# CLAUDE.md — docs-web 작업 가이드

SceneMakerAI 기술 문서 사이트의 작업 가이드. 코드 개발 저장소가 아닌 **콘텐츠 사이트**다. 작업의 대부분은 Markdown 추가·수정과 EN 번역이다.

---

## 프로젝트 개요

**SceneMakerAI**는 오픈소스 AI(멀티모달 LLM)로 방송 콘텐츠(드라마·예능·다큐멘터리)를 재가공하는 **솔박스(Solbox Inc.) 사내 프로젝트**다. 2026 NIPA 오픈소스 AI·SW 지원사업으로 진행 중이며, 4대 서비스(모아보기·리믹스·광고·Batch)와 오픈소스 기여 30+건을 목표로 한다.

- 운영 URL: `https://doc.scenemaker.solbox.com`
- GitHub: `https://github.com/SceneMakerAI/docs-web`
- 기술 스택: **Docusaurus 3.10.1** (React 19, TypeScript 6)
- 기본 언어: 한국어(KR), 번역: 영어(EN)
- 콘텐츠 원본: **Notion DB** → GitHub Actions로 자동 동기화
- 참고 문서: https://docusaurus.io/ko/docs
---

## 디렉토리 구조

```
docs-web/
├── docs/                          # KR 원본 문서 (Notion 자동 동기화 대상)
│   ├── about/                     # 프로젝트 소개
│   ├── architecture/              # 아키텍처 설계
│   ├── contribute/                # 오픈소스 기여 가이드
│   ├── guide/                     # 시작하기 / 문서 인덱스
│   ├── install/                   # 설치 가이드 (Qwen, LLM 등)
│   ├── poc/                       # PoC 기획안 및 벤치마크
│   └── release-notes/             # 릴리즈 노트
│
├── blog/                          # 블로그 (독립 blog 플러그인, Notion 자동 동기화 대상)
│   ├── authors.yml                # 저자 프로필 목록
│   └── YYYY-MM-DD-slug.md         # 블로그 포스트 (날짜-슬러그 형식)
│
├── docs_en/                       # EN 번역 문서 (개발자 편집 위치)
│   └── (docs/ 와 동일한 서브디렉토리 구조)
│
├── i18n/
│   └── en/
│       ├── docusaurus-plugin-content-docs/
│       │   └── current -> ../../../docs_en   ← symlink (docs_en/ 가리킴)
│       ├── docusaurus-theme-classic/
│       │   ├── navbar.json        # EN 네비바 라벨
│       │   └── footer.json        # EN 푸터 라벨
│       └── code.json              # EN 컴포넌트 UI 문자열
│
├── src/
│   ├── pages/index.tsx            # 홈페이지 (Hero + Features + KPI)
│   ├── components/
│   │   ├── HomepageFeatures/      # 기능 소개 카드
│   │   └── HomepageKPI/           # KPI 지표 섹션
│   └── css/custom.css             # 전역 CSS 토큰
│
├── static/
│   ├── img/                       # 정적 이미지 (섹션별 하위 폴더)
│   └── CNAME                      # doc.scenemaker.solbox.com
│
├── scripts/
│   ├── server-sync.sh             # 서버 crontab 실행 진입점 (Notion 동기화 전체 오케스트레이션)
│   ├── notion_to_md.py            # Notion DB → docs/ · blog/ 범용 동기화 (계층 구조 지원, blog 모드 포함)
│   ├── translate_to_en.py         # 변경된 docs/ → DeepL 번역 → docs_en/ 저장
│   ├── md_to_notion.py            # docs/*.md 변경 → Notion DB 업로드
│   ├── md_to_notion.sh            # md_to_notion.py 실행 래퍼
│   ├── sync-develop.sh            # main 최신 콘텐츠를 develop으로 즉시 흡수 (로컬 수동용)
│   ├── sync-local.sh              # 로컬 Notion 전체 동기화 (개발용)
│   ├── deploy.sh                  # GH Actions 수동 트리거 래퍼 (레거시, 미사용 워크플로우 참조)
│   ├── sync.sh                    # Notion → contribute/ 동기화 후 즉시 push (레거시)
│   ├── notion_to_blog.py          # 블로그 전용 동기화 (레거시)
│   └── notion_to_contribute.py    # 기여 전용 동기화 (레거시)
│
├── .github/workflows/
│   ├── deploy.yml                 # main push 시 GH Pages 자동 배포
│   ├── md-to-notion.yml           # docs/**/*.md push 시 Notion DB 역업로드
│   ├── sync-develop.yml           # 매일 KST 03:00 — main 콘텐츠를 develop으로 흡수
│   ├── merge-develop.yml          # 매일 KST 11:00 — develop 코드 변경을 main으로 자동 머지
│   └── pr-build.yml               # main·develop PR 시 프로덕션 빌드 검증
│
├── docusaurus.config.ts           # 사이트 설정 (i18n, navbar, footer, webpack)
├── sidebars.ts                    # 7개 사이드바 정의 (autogenerated, 블로그 제외)
└── .aidocs/design.md              # 미래 리디자인용 토큰 참조 (Revolut 스타일, 현재 미적용)
```

---

## i18n 구조 핵심

**한국어가 원본, 영어는 번역본.**

| 경로 | 역할 |
|------|------|
| `docs/` | KR 원본. Notion 자동 동기화 대상. |
| `docs_en/` | EN 번역본 편집 위치. **실제 파일이 여기 있다.** |
| `i18n/en/.../current` | `docs_en/`을 가리키는 심볼릭 링크. Docusaurus가 읽는 경로. |

### 주의: symlink 방향

```
i18n/en/docusaurus-plugin-content-docs/current  →  ../../../docs_en
```

- `docs_en/` = 실제 파일
- `i18n/.../current` = symlink

**반대로 하면 안 된다.** webpack이 symlink를 real path로 resolve해서 Docusaurus SSG 라우팅과 불일치 → DocItem `content` props undefined 오류 발생. `docusaurus.config.ts`의 `resolve.symlinks: false` 플러그인이 이 문제를 해결한다 (방향이 맞더라도 적용 중).

### EN 파일 추가 규칙

1. `docs/section/filename.md`에 KR 파일이 있으면 → `docs_en/section/filename.md` 에 EN 번역 추가
2. EN 파일 frontmatter에 `slug: "N"` 반드시 포함 (KR과 동일한 값)
3. EN 파일에 `id:` 필드는 넣지 않는다 — `translate_to_en.py`가 자동으로 제거하며, Docusaurus가 파일 경로로 KR/EN 매칭
4. `sidebar_position:` 은 KR과 동일하게
5. **`docs_en/` 파일명은 KR 파일명과 반드시 동일해야 한다** — Docusaurus가 파일 경로 기준으로 KR/EN 페이지를 매칭하므로 이름이 다르면 로케일 스위처가 깨짐
   - KR: `docs/poc/poc-기획안-멀티모달-llm.md` → EN: `docs_en/poc/poc-기획안-멀티모달-llm.md` (동일 파일명 필수)
   - Notion 동기화로 KR 파일명이 변경되면 EN 파일명도 반드시 함께 변경할 것
   - `translate_to_en.py`가 자동으로 처리하지만, 수동으로 EN 파일 생성 시에도 동일하게 적용

---

## URL 구조 (slug 시스템)

Notion 동기화 파일은 `slug: "N"` frontmatter로 숫자 URL을 갖는다.

```
docs/architecture/아키텍처-설계.md  (slug: "1")  →  /docs/architecture/1
docs/install/qwen-3x-설치.md       (slug: "1")  →  /docs/install/1
docs/install/llm-설치.md           (slug: "2")  →  /docs/install/2
```

**계층 구조 페이지 (Notion 하위 항목이 있는 경우):**

```
docs/poc/vision-bench/_category_.json  (generated-index, slug: "/poc/vision-bench")  →  /docs/poc/vision-bench
docs/poc/vision-bench/child.md         (slug: "1")                                    →  /docs/poc/vision-bench/1
```

- 카테고리 인덱스 URL은 **`_category_.json`의 `generated-index`** 가 담당한다.
  - `"link": {"type": "generated-index", "slug": "/{section}/{subdir}"}` 형식으로 명시적 slug 지정 필수
  - ⚠️ `slug: "/"` 사용 금지 — Docusaurus에서 절대경로(`/docs/`)로 해석되어 Duplicate routes 발생
- 부모 `index.md`는 Notion 페이지에 **본문 내용이 있을 때만** 생성된다 (body 없으면 자동 삭제).
  - `index.md`가 있을 경우 `slug`는 디렉토리명(`"vision-bench"`)으로 설정하나, Docusaurus는 이를 **하위 디렉토리 기준 상대 경로**로 처리해 실제 URL은 `/docs/poc/vision-bench/vision-bench`가 된다 — `_category_.json`과 중복 생성되지 않도록 주의.
  - ⚠️ `"type": "doc"` + `"id": "..."` 조합은 slug 충돌 시 잘못된 URL로 이동
- 자식 파일은 `slug: "{child-order}"` (부모 디렉토리 기준 상대 슬러그)
- **slug는 섹션 내 상대경로**. `"architecture/1"` 같은 전체 경로 쓰면 안 된다.
- 새 파일은 섹션 내에서 `sidebar_position` 순서에 맞춰 다음 번호를 부여한다.
- **Notion 동기화 파일**: `scripts/notion_to_md.py`가 `slug` 자동 생성. 수동 추가 파일만 직접 slug를 넣어야 한다.

---

## Notion 동기화

### 실제 운영 방식 — 서버 crontab

GitHub Actions가 아닌 **서버에서 직접 crontab으로 30분마다** 실행한다.

```
*/30 * * * * /root/docs-web/scripts/server-sync.sh >> /var/log/notion-sync.log 2>&1
```

**`scripts/server-sync.sh` 흐름:**

1. `git pull --rebase origin main` — 원격 최신 코드 반영
2. 8개 Notion DB 병렬 동기화 (`notion_to_md.py`, `FETCH_MODE=ALL`)
   - `docs/about|architecture|contribute|guide|install|poc|release-notes/` (7개) → docs 플러그인
   - `blog/` (1개) → blog 플러그인 frontmatter 형식 (`date`, `authors`, `tags` 등 자동 생성)
3. `translate_to_en.py` — 변경된 KR 파일 DeepL 번역 → `docs_en/` 저장
4. 변경사항 있으면 `git commit + push` → `deploy.yml` 트리거 → GH Pages 배포

환경변수는 `/root/docs-web/.env` 파일에서 로드한다 (레포에 포함되지 않음).

로그 확인:
```bash
tail -f /var/log/notion-sync.log
```

> GitHub Actions 워크플로우: `sync-develop.yml`이 매일 KST 03:00에 main 콘텐츠를 develop으로 자동 흡수하고, `merge-develop.yml`이 매일 KST 11:00에 develop의 코드 변경을 main으로 자동 머지한다. 평소 콘텐츠 동기화는 서버 crontab이 담당한다.

---

### DB ↔ 디렉토리 매핑

| `.env` 변수 | 저장 경로 | 비고 |
|------------|-----------|------|
| `NOTION_ABOUT` | `docs/about/` | docs 플러그인 |
| `NOTION_ARCHITECTURE` | `docs/architecture/` | docs 플러그인 |
| `NOTION_BLOG` | `blog/` | blog 플러그인 (frontmatter: date·authors·tags·description·image) |
| `NOTION_CONTRIBUTE` | `docs/contribute/` | docs 플러그인 |
| `NOTION_DOCS` | `docs/guide/` | docs 플러그인 |
| `NOTION_INSTALL` | `docs/install/` | docs 플러그인 |
| `NOTION_POC` | `docs/poc/` | docs 플러그인 |
| `NOTION_RELEASE` | `docs/release-notes/` | docs 플러그인 |
| `NOTION_TOKEN` | — | API 인증 토큰 |
| `DEEPL_API_KEY` | — | DeepL 번역 API 키 |

### 동기화 내부 로직 (`notion_to_md.py`)

**docs 모드** (`SAVE_DIR=docs/...`): Notion "하위 항목" relation을 읽어 자동으로 계층 구조를 결정한다.

- 하위 항목 없는 페이지 → `docs/{section}/{slug}.md` (평면)
- 하위 항목 있는 부모 → `docs/{section}/{slug}/index.md` + `_category_.json` 자동 생성
- 자식 페이지 → `docs/{section}/{parent-slug}/{child-slug}.md`

**blog 모드** (`SAVE_DIR=blog`): Notion 속성을 blog 플러그인 frontmatter로 변환한다.

- 파일명 → `blog/{date}-{slug}.md`
- frontmatter → `title`, `date`, `authors`, `description`, `tags`, `image` 자동 생성
- Notion 속성명 기본값: `date`, `authors`, `description`, `tags`, `image` (env로 재정의 가능)

| 환경변수 | 기본값 | 설명 |
|---------|--------|------|
| `NOTION_PROPERTY_SUBITEM` | `하위 항목` | Sub-items relation 속성명 |
| `NOTION_PROPERTY_PARENT` | `상위 항목` | Parent item relation 속성명 |

### 수동 동기화 (단일 섹션)

```bash
NOTION_TOKEN=... NOTION_DATABASE_ID=... SAVE_DIR=docs/guide python3 scripts/notion_to_md.py
```

---

## 자주 쓰는 명령어

| 명령어 | 용도 |
|--------|------|
| `npm start` | KR dev 서버 (port 3000) |
| `npm run start:en` | EN dev 서버 (port 3002) |
| `npm run build` | 프로덕션 빌드 (KR + EN 동시) |
| `npm run serve` | build/ 결과 로컬 서빙 |
| `npm run clear` | Docusaurus 캐시 정리 |
| `npm run typecheck` | TypeScript 타입 체크 |
| `npm run write-translations:en` | EN 번역 JSON 골격 재생성 |

---

## 빌드 게이트

### CI에서 실패하는 것
- **깨진 내부 링크** — `onBrokenLinks: 'throw'` 설정. PR 전 `npm run build` 통과 필수.
- **MDX 컴파일 오류** — 프론트매터·JSX 문법 오류.

### 로컬에서만 확인
- `npm run typecheck` — `strict: true`지만 빌드와 무관. IDE 보조용.

---

## 브랜치 전략

| 작업 유형 | 브랜치 | 방법 |
|----------|--------|------|
| **콘텐츠 변경** (Markdown 추가·수정, EN 번역) | `main` 직접 커밋 | Notion 자동 동기화 포함 |
| **버그픽스·CI 수정·설정 소폭 변경** | `main` 직접 커밋 | 영향 범위가 작고 명확한 경우 |
| **기능 추가** (새 기능·컴포넌트·스크립트 신규) | `feat/<이름>` 브랜치 | 작업 후 main에 merge, 브랜치 삭제 |

```
# 기능 추가 시
git checkout -b feat/<이름>
git rebase origin/main             # 작업 전 반드시 main 최신 상태 반영
# ... 작업 ...
git push origin feat/<이름>
git checkout main
git pull --rebase origin main      # merge 전 main 최신화
git merge feat/<이름>
git push origin main
git branch -d feat/<이름>          # 로컬 브랜치 정리
git push origin --delete feat/<이름>  # 원격 브랜치 정리
```

> Notion 자동 동기화가 `main`에 직접 커밋하므로, `feat/` 브랜치 작업 시작 전 `rebase origin/main`을 생략하면 merge conflict가 발생한다.

**자동화 워크플로우 (수동 개입 불필요):**
- `sync-develop.yml` — 매일 KST 03:00에 main 콘텐츠를 develop으로 자동 흡수 (`.notion-sync.json` 충돌 자동 해소)
- `merge-develop.yml` — 매일 KST 11:00에 develop의 코드 변경(docs·docs_en·blog·static 제외)을 main으로 자동 머지 후 배포

---

## 콘텐츠 추가 워크플로우

### 새 Notion 섹션 추가 시

1. `secrets.NOTION_XXX` 시크릿 등록
2. `scripts/server-sync.sh`에 새 DB 환경변수 블록 추가
3. `docs/new-section/` 디렉토리 및 `_category_.json` 생성
4. `sidebars.ts`에 새 사이드바 항목 추가
5. `docusaurus.config.ts` navbar에 `{type: 'docSidebar', ...}` 추가

### 수동으로 새 문서 추가 시

1. `docs/section/filename.md` 생성 (slug, id, sidebar_position, title 포함)
2. `docs_en/section/filename.md` 생성 (동일 frontmatter + 영어 내용)
3. `npm run build` 통과 확인 후 main에 push

### 빈 섹션 navbar 등록 시 주의

`type: 'docSidebar'` navbar 항목은 해당 섹션에 `.md` 파일이 하나 이상 있어야 빌드 통과.
Notion 콘텐츠가 아직 없는 섹션을 navbar에 추가할 때는 반드시 플레이스홀더 파일을 먼저 생성할 것.

```
docs/{section}/intro.md        ← KR 플레이스홀더 (Notion sync가 삭제하지 않음)
docs_en/{section}/intro.md     ← EN 플레이스홀더
```

플레이스홀더 frontmatter: `slug: "intro"`, `sidebar_position: 1` 사용.
Notion에 실제 콘텐츠가 추가되면 자동 동기화되어 함께 표시된다.

---

## 사이드바 목록

| 사이드바 ID | 네비바 라벨 | `docs/` 디렉토리 |
|------------|------------|-----------------|
| `aboutSidebar` | 프로젝트 소개 | `about/` |
| `architectureSidebar` | 아키텍처 | `architecture/` |
| `installSidebar` | 설치 | `install/` |
| `pocSidebar` | PoC | `poc/` |
| `docsSidebar` | 문서 | `guide/` |
| `contributeSidebar` | 오픈소스 기여 | `contribute/` |
| `releaseNotesSidebar` | 릴리즈 노트 | `release-notes/` |

> 블로그는 독립 blog 플러그인이 자체 사이드바를 관리하므로 `sidebars.ts`에 포함되지 않는다. navbar 링크는 `{to: '/blog'}` 방식.

---

## 하지 말 것

- `docusaurus.config.ts`의 `url` / `baseUrl` / `organizationName` / `projectName` 임의 변경 금지 (GH Pages 배포와 RSS 절대 URL에 직접 영향)
- `onBrokenLinks: 'throw'`를 `'warn'`으로 낮추지 말 것
- EN 번역 만든다고 KR 원본(`docs/`)을 영어로 덮어쓰지 말 것
- `i18n/en/.../current` 를 real directory로 바꾸지 말 것 (symlink여야 함)
- `.docusaurus/`, `build/`, `node_modules/` 커밋 금지
- **`_category_.json` 수동 편집 시 주의**:
  - **섹션 루트** (`docs/{section}/_category_.json`): 수동 관리. `link.type: "doc"` 사용 시 실제 존재하는 doc ID인지 반드시 확인 (`npm run build` 로컬 통과 필수)
  - **서브디렉토리** (`docs/{section}/{subdir}/_category_.json`): `notion_to_md.py`가 자동 생성·관리. 직접 수정하면 다음 sync에 덮어씌워짐
  - 부모 `index.md`에 `id:` 필드 추가 금지 — `_category_.json`의 `link.id`가 파일 경로 기반 ID(`{section}/{slug}/index`)로 고정되어 있어 충돌 발생
- **KR 파일이 이동(rename)된 경우** 대응하는 stale EN 파일을 수동 삭제해야 함 — `translate_to_en.py`는 git diff 삭제 감지만 하므로 rename은 자동 처리 안 됨
